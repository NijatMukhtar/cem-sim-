#include <stdio.h>

/* HAL stub: ileride hal_stm32.c ile değiştirilecek */
void activate_EM(int idx){ (void)idx; /* TODO: HAL set pin */ }
void deactivate_EM(int idx){ (void)idx; /* TODO: HAL reset pin */ }
